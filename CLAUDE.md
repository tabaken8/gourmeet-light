# Gourmeet Lite

Next.js 15 (App Router, Turbopack) + Supabase + React 19 のグルメSNSアプリ。

## 開発コマンド

```bash
npm run dev        # 開発サーバー起動
npm run build      # プロダクションビルド
npm run lint       # ESLint
```

## 進め方

- 確認のための質問はしない。曖昧な点は妥当な仮定を置いて進め、置いた仮定は作業後にまとめて報告する。
- 実装方針の事前承認を求めない。まず動くものを作ってから見せる。
- 途中経過の実況はしない。まとまった単位で完了報告だけする。
- 中断してよいのは、破壊的操作が必要な場合か、仕様が根本的に矛盾している場合のみ。
